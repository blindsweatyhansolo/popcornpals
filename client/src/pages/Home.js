import MovieList from '../components/MovieList';
import SuggestionList from '../components/SuggestionList';

const Home = () => {
  return (
    <div>
      HOMEPAGE
    </div>
  );
};

export default Home;